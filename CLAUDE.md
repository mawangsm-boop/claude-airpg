# CLAUDE.md 에 추가할 조항 (핸드북 원격 동기화)

아래 블록을 저장소 `CLAUDE.md` 최상단, [조종권] 등 최우선 규칙 바로 다음에 붙여넣는다.

---

[핸드북 동기화 규칙] (최우선 — 이 규칙 없이 상태 변경을 끝내지 않는다)
- 게임 상태는 `state/*.json` 이 유일한 원본이다. 상태가 바뀌면 해당 파일을 직접 수정한다.
- **상태 파일을 고쳤으면 반드시 같은 커밋에서 `state/rev.json` 의 rev 값을 바꾼다.**
  rev를 안 바꾸면 사용자 화면이 갱신되지 않으므로, 이걸 빠뜨리면 작업 미완료로 간주한다.
  형식: { "rev": "S{회차}-{일련번호}", "updated": ISO시각, "note": "무엇을 바꿨는지" }
- 갱신 시점:
  - 전투 중 HP·임시HP·주문 슬롯·토큰 위치 → 매 턴 즉시 (battle.json, core.json)
  - 소지품·골드 변동 → 발생 즉시 (core.json)
  - 대륙 지도(world.json) → 새 지역으로 이동하거나 발견했을 때만
  - 던전 구역 이동 → dungeon.json
  - 로그(log.json) → 마을↔던전 같은 큰 구역 전환 시 모아서
- 던전·전투 종료 시 파일을 삭제하지 않는다. 빈 상태로 되돌린다
  (battle.tokens: [], battle.round: 1). 파일이 없으면 사용자 화면 로딩이 실패한다.
- 스키마를 임의로 바꾸지 않는다. HP는 {cur, max, tmp}, 슬롯은 {cur, max, lvl},
  party/npcs 항목은 name/role/trait 키를 쓴다(n/d 축약 금지).
- 커밋·푸시 완료를 보고할 때 어떤 파일을 고쳤고 rev를 무엇으로 올렸는지 함께 알린다.
